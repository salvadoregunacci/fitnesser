
export enum Screen {
    start = "start",
    wormup = "wormup",
    press = "press",
    pushups = "pushups",
    plank = "plank",
    squats = "squats",
    stretch = "stretch",
    finish = "finish",
}

export type ITime = {
    minutes: number,
    seconds: number,
}